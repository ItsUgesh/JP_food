{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account with specific roles for accessing the POS system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the system (e.g., \"admin\", \"staff\")."
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "role"
      ]
    },
    "MenuItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MenuItem",
      "type": "object",
      "description": "Represents a single item available on the cafe's menu.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the MenuItem entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the menu item."
        },
        "price": {
          "type": "number",
          "description": "The price of the menu item."
        },
        "category": {
          "type": "string",
          "description": "The category the menu item belongs to (e.g., Drinks, Snacks, Meals)."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the menu item was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "price",
        "category",
        "createdAt"
      ]
    },
    "OrderItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrderItem",
      "type": "object",
      "description": "Represents a single item within an order, capturing its state (name, price, quantity) at the time of ordering.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the OrderItem entity."
        },
        "orderId": {
          "type": "string",
          "description": "Reference to the parent Order. (Relationship: Order 1:N OrderItem)"
        },
        "name": {
          "type": "string",
          "description": "The name of the item at the time it was added to the order."
        },
        "price": {
          "type": "number",
          "description": "The price of the item at the time it was added to the order."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of this item included in the order."
        }
      },
      "required": [
        "id",
        "orderId",
        "name",
        "price",
        "quantity"
      ]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents a customer order placed in the cafe, linking to its individual order items.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Order entity."
        },
        "tableNumber": {
          "type": "string",
          "description": "The table number associated with the order."
        },
        "orderItemIds": {
          "type": "array",
          "description": "References to OrderItem entities that belong to this order. (Relationship: Order 1:N OrderItem)",
          "items": {
            "type": "string"
          }
        },
        "total": {
          "type": "number",
          "description": "The total calculated price of all items in the order."
        },
        "status": {
          "type": "string",
          "description": "The current status of the order (e.g., \"pending\", \"paid\")."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the order was created.",
          "format": "date-time"
        },
        "paymentMethod": {
          "type": "string",
          "description": "The method used for payment (e.g., \"cash\", \"eSewa\"). This field is populated only if the order status is 'paid'."
        },
        "paidAt": {
          "type": "string",
          "description": "Timestamp indicating when the order was marked as paid. This field is populated only if the order status is 'paid'.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "tableNumber",
        "orderItemIds",
        "total",
        "status",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "A lookup collection for admin roles.",
          "params": [
            {
              "name": "userId",
              "description": "User ID."
            }
          ]
        }
      },
      {
        "path": "/menu_items/{menuItemId}",
        "definition": {
          "entityName": "MenuItem",
          "schema": {
            "$ref": "#/backend/entities/MenuItem"
          },
          "description": "Stores menu items.",
          "params": [
            {
              "name": "menuItemId",
              "description": "Menu item ID."
            }
          ]
        }
      },
      {
        "path": "/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Stores customer orders.",
          "params": [
            {
              "name": "orderId",
              "description": "Order ID."
            }
          ]
        }
      }
    ]
  }
}